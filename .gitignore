# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist-ssr
*.local
# dist/ is deliberately tracked, not ignored -- see the root .gitignore's
# note and docs/cpanel-deployment.md. It's SSR builds (dist-ssr) that stay
# ignored here; this project doesn't use SSR at all, kept only in case a
# future `vite build --ssr` run produces one.

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
